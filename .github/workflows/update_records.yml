name: Update CF records

on:
  push:
    branches: [ "main" ]

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: 1.18

    - name: Run cafe
      run: go build cmd/cafe/main.go
